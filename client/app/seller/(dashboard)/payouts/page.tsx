import { SellerPayoutsClient } from "@/components/seller/SellerPayoutsClient";

/** `/seller/payouts` — earnings summary + payout history. */
export default function SellerPayoutsPage() {
  return <SellerPayoutsClient />;
}

import { SellerReviewsClient } from "@/components/seller/SellerReviewsClient";

/** `/seller/reviews` — read + reply to reviews on this maker's products/vendor. */
export default function SellerReviewsPage() {
  return <SellerReviewsClient />;
}

import { SellerAnalyticsClient } from "@/components/seller/SellerAnalyticsClient";

/** `/seller/analytics` (M16) — what is selling, and when. */
export default function SellerAnalyticsPage() {
  return <SellerAnalyticsClient />;
}

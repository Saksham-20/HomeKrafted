import { MealDeliveriesClient } from "@/components/seller/MealDeliveriesClient";

/** `/seller/meal-plans/deliveries` — the full fortnight of meals this kitchen owes. */
export default function SellerMealDeliveriesPage() {
  return <MealDeliveriesClient />;
}

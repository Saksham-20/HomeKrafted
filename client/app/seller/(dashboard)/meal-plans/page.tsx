import { MealPlansClient } from "@/components/seller/MealPlansClient";

/** `/seller/meal-plans` — this kitchen's subscription plans and the meals they owe. */
export default function SellerMealPlansPage() {
  return <MealPlansClient />;
}

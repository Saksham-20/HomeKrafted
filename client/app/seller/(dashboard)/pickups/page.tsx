import { PartnerPickupsClient } from "@/components/seller/PartnerPickupsClient";

/** `/seller/pickups` (M10b, laundry type) — bookings assigned to this partner. */
export default function SellerPickupsPage() {
  return <PartnerPickupsClient />;
}

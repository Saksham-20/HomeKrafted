import { SellerMenuEditorClient } from "@/components/seller/SellerMenuEditorClient";

/** `/seller/menu/new` (M10b, snack type) — create a snack. */
export default function NewSellerMenuItemPage() {
  return <SellerMenuEditorClient />;
}

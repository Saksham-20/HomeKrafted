import { SellerMenuClient } from "@/components/seller/SellerMenuClient";

/** `/seller/menu` (M10b, snack type) — this seller's `Snack`s, owner-scoped client state. */
export default function SellerMenuPage() {
  return <SellerMenuClient />;
}

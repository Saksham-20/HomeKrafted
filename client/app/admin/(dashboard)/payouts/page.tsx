import { PayoutsClient } from "@/components/admin/PayoutsClient";

/** `/admin/payouts` (M15) — the settlement queue for HomeKrafter earnings. */
export default function AdminPayoutsPage() {
  return <PayoutsClient />;
}

import { CollectionsClient } from "@/components/admin/CollectionsClient";

/** `/admin/collections` — every occasion collection, create/edit. */
export default function AdminCollectionsPage() {
  return <CollectionsClient />;
}

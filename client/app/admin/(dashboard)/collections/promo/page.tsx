import { HomePromoEditorClient } from "@/components/admin/HomePromoEditorClient";

/** `/admin/collections/promo` — edit the home page's two promo bands. */
export default function AdminHomePromoPage() {
  return <HomePromoEditorClient />;
}

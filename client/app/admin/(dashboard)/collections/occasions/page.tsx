import { OccasionsClient } from "@/components/admin/OccasionsClient";

/** `/admin/collections/occasions` (M16) — festival dates and hub taglines. */
export default function AdminOccasionsPage() {
  return <OccasionsClient />;
}

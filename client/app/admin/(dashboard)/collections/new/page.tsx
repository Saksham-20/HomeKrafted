import { CollectionEditorClient } from "@/components/admin/CollectionEditorClient";

/** `/admin/collections/new` — create a collection. */
export default function AdminNewCollectionPage() {
  return <CollectionEditorClient />;
}

import { WalletOverviewClient } from "@/components/admin/WalletOverviewClient";

/** `/admin/wallet` — platform-wide wallet liability + per-user balances. */
export default function AdminWalletPage() {
  return <WalletOverviewClient />;
}

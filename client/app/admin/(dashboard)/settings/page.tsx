import { SettingsClient } from "@/components/admin/SettingsClient";

/** `/admin/settings` (M16) — platform values that were constants in source. */
export default function AdminSettingsPage() {
  return <SettingsClient />;
}

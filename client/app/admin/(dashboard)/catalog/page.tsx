import { CatalogClient } from "@/components/admin/CatalogClient";

/** `/admin/catalog` — every product across every vendor, unscoped, moderation actions. */
export default function AdminCatalogPage() {
  return <CatalogClient />;
}

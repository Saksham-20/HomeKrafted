import { AdminListingCreateClient } from "@/components/admin/AdminListingCreateClient";

/** `/admin/catalog/new` — an admin lists a product, as Homekrafted or on a HomeKrafter's behalf (M44). */
export default function AdminCatalogCreatePage() {
  return <AdminListingCreateClient />;
}

import { CorporateInquiriesClient } from "@/components/admin/CorporateInquiriesClient";

/** `/admin/corporate` — the bulk-gifting queue (M20). */
export default function AdminCorporatePage() {
  return <CorporateInquiriesClient />;
}

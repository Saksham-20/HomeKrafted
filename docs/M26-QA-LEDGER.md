# M26 — QA sweep ledger

Findings, appended live. The plan is `docs/M26-QA-PLAN.md`; the severity
predicates are its §2 and the judgement criteria are its §7.1 and §7.2.

**Coverage does not live here.** A clean route produces zero rows, which is
indistinguishable from a route nobody opened — so "what is left" is
`docs/route-inventory.tsv`, not this file:

```bash
grep -c '	—	' docs/route-inventory.tsv     # routes still unswept
grep -c '^M26-' docs/M26-QA-LEDGER.md         # findings against the 80 budget
```

**Single writer.** A second sweeper files into `docs/M26-QA-LEDGER-<name>.md`
and prefixes their IDs (`B-001`), merged at wave close. Two sessions
appending to one markdown file is a merge conflict on every row.

**Severity is written before the fix is estimated** (plan §2), so a cheap
fix cannot inflate its own priority. P0 money/data/security/dead-end · P1
blocked with no workaround · P2 wrong or confusing, with a workaround ·
P3 polish. A design defect that prevents the primary action being found or
performed is P1; one that merely makes the screen worse is a capped Q1
proposal, filed in that wave's proposals section rather than here.

**A finding is closed by a diff or by a written deferral**, never by an
opinion. The Fix column records the command that reproduces the failure on
the parent commit — stash the fix, run the new spec, paste the red output.

---

| ID | Wave | Route/flow | Persona | Viewport | Sev | Rule | What happened | Expected | Evidence | Dupe-of | Fix / deferred + why |
|---|---|---|---|---|---|---|---|---|---|---|---|
| M26-001 | 0 | `e2e/` browser layer | — | both | **P1** | plan §11 (a rule with no test is a preference) | `auth.setup.ts` drove the two-tab login form M25 deleted; two of three setup steps timed out and every dependent test **skipped**, so the reporter printed "0 failed". CI could not catch it either — the browser job set `JWT_SECRET` where `env.validation.ts` requires `JWT_ACCESS_SECRET`, so its API never booted. | The suite runs, and a broken fixture fails loudly rather than skipping. | parent commit: 2 failed / 30.4s; fixed: 129 passed / 22.8s | — | **Fixed** `eefc155`. Selectors moved to `e2e/fixtures/sign-in.ts` (one place — the duplication across the setup and six blocks of `error-paths.spec.ts` is why it drifted), anchored on the placeholder because the label relabels itself as you type, throwing a named diagnosis instead of timing out. |
| M26-002 | 0 | `/shop` filters | any | 390 | P2 | `e2e/README.md` (isVisible does not wait) | `openFilters` probed with `isVisible()`, an instant check that answers false mid-hydration. The click never happened and the failure surfaced 30s later at the checkbox. Product verified fine at 390px first — this was the test. | The helper waits for whichever shape the viewport renders. | 3 mobile tests, all green after | — | **Fixed** `eefc155`. Mobile-only because desktop has no toggle to miss. |
| M26-003 | 0 | `/admin/orders` | admin | both | P2 | — | The "search reaches an order not on the first page" test can never pass against the documented dataset: 21 rows across orders, laundry bookings and snack orders, against `DEFAULT_ORDER_PAGE_SIZE` 25. It waited for a "Next" that was correctly absent. | A precondition the seed can actually produce. | — | — | **Fixed** `eefc155` — `prisma/seed-browser-orders.ts`, browser-stack only, kept out of `seed.ts` so 20 filler orders stay out of every tester's history. |
| M26-004 | 0 | `/account/orders` | first-time buyer | both | P2 | plan §7.1 (no live copy names a withdrawn module) | The subtitle read "Marketplace orders and laundry bookings, in one place", the empty state read "bookings made on **Laundry** will show up here", and a Laundry filter chip sat above both. Laundry was withdrawn in M19 and `/laundry` 404s — so the first screen a new account saw pointed three times at a module that does not exist. | Copy that names only what this buyer can still do. | `OrdersListClient.tsx:49,70` + the chip at `:18`; verified in a browser on both a fresh account and one with bookings | — | **Fixed.** All three are now conditional on the account actually having a booking — deleting the word outright would have lost six real bookings their only filter. The empty state also gained its third part (a way out). Spec: `e2e/tests/withdrawn-modules.spec.ts`; red against the parent (`git stash push -- client/components/account/OrdersListClient.tsx`), green after. |
| M26-005 | 0 | docs | — | — | P3 | `CLAUDE.md` docs upkeep | `PRODUCTION-AUDIT.md` L3 says `/gallery` is "publicly routable in production". `client/app/gallery/page.tsx:57` calls `notFound()` when `NODE_ENV === "production"`. `DESIGN-SYSTEM.md` is right; the audit is stale. | Docs that do not contradict the source. | — | — | **Open**, Wave 0.7. Deletes a Wave 1 decision — there is nothing to gate. |
| M26-006 | 0 | docs | — | — | P3 | `CLAUDE.md` docs upkeep | `CLAUDE.md` and `PRODUCTION-AUDIT.md` M3 both say the axe suite runs "over every public route at both viewports". It runs over 7 of ~31, and `presentation.spec.ts` has a *different* list of 8 (it has `/hamper`, a11y does not). | One derived list, and docs that describe it accurately. | `e2e/tests/a11y.spec.ts:23`, `presentation.spec.ts:14` | — | **Open**, Wave 0.8 / T4. |
| M26-007 | 0 | `CHANGELOG.md` | — | — | P3 | `CLAUDE.md` docs upkeep | No M22, M23 or M24 entry; the newest-but-one heading is `[M21] … (in progress)` holding what `CLAUDE.md` calls M23. | A ledger that names what shipped. | — | — | **Open**, Wave 0.7. |
| M26-008 | 1 | `/admin/orders/[type]/[id]` | admin | both | **P0** | plan §2 (money moves wrongly — refunded twice) | The refund button posted a raw wallet credit (`POST /admin/wallet/:userId/refund`) with an operator-typed amount, **no `Idempotency-Key`**, and `refId` set to the order *number* rather than its id. It never set `Order.refundStatus`, so the same order could be refunded again on the next visit with nothing on the screen saying it already had been. The purpose-built `POST /admin/orders/:type/:id/refund` — idempotent, audited, guarded on `refundStatus`, refuses an unpaid order — existed and was unused. M23 fixed this exact class on the buyer's "Place order" and the admin path was never revisited. | One credit per order, whatever happens to the network. | Reproduced against the running API: three calls to the old path on `ord-seed-1987` took the wallet 2749 → 7246, +₹4,497 on a ₹1,499 order, `refundStatus` already `refunded` throughout. | — | **Fixed.** `refundAdminOrder()` calls the audited endpoint with a key minted once per mounted order (a per-*click* key does not help with the retry-after-timeout case). The free-text amount is gone — it implied a partial refund the ledger has no concept of reversing; partial adjustments keep their own screen at `/admin/wallet/[userId]`, which is the path `CLAUDE.md` already documents. Spec: `server/test/e2e/admin-order-detail.e2e-spec.ts`. |
| M26-009 | 1 | `/admin/orders/[type]/[id]` | admin | both | **P1** | plan §2 (blocked, no workaround) | The screen resolved its header by fetching page 1 of `GET /admin/orders` and finding the row in the browser. The list pages at 25 per source, so **any order with 25 newer siblings rendered "Order not found."** — for a record the API returns happily. The refund control is on that screen, so in practice an admin could not refund an order once the queue moved past it. Identical shape to `8298b4b` (`/admin/catalog/[id]` resolving out of the *public* catalogue); that fix was not generalised. | The detail screen opens any order that exists. | `GET /api/v1/admin/orders/marketplace/ord-seed-1987` returned the order while the page said "Order not found." Screenshots before/after in `.qa-shots/`. | — | **Fixed.** New `GET /admin/orders/:type/:id/summary`, built by the same `listX` row-builders so a summary can never disagree with the list row it was reached from. Spec red on the parent (4 failed), green after. |
| M26-010 | 1 | 24 page-visits across shop, product, account, seller, admin | any | both | **P1** | `CLAUDE.md` token rules (gold is never for text) + plan §2 (axe contrast) | 114 axe `color-contrast` nodes. `--hk-gold` as a text colour measured **2.88:1** on the product page's maker line and 3.2:1 on the gift-wrap label, the `/sell` section labels, the wallet and referral eyebrows, the seller editors. The 2026-08-08 audit fixed exactly the 7 routes in `a11y.spec.ts`'s `PUBLIC_ROUTES` — which is M26-006's finding cashed out: what is not measured is not fixed. Also `--hk-whatsapp` under white on the button that starts every Snacks order (3.09:1), and terracotta on tinted panels (3.87–4.09:1). | AA everywhere, including the surfaces nobody automated. | Sweep pass 1: 114 nodes / 24 page-visits. Pass 3: **0 on every production route.** | — | **Fixed.** 31 stylesheets moved to `--hk-gold-text-sm` / `--hk-terracotta-text` / `--hk-whatsapp-text`, the tokens that exist for this. Three star-rating rules deliberately keep `--hk-gold` with a comment saying why (a filled star is a graphic at 3:1, and the rating is stated in text beside it). |
| M26-011 | 1 | every listing/menu/meal-plan/storefront/profile editor | HomeKrafter | both | **P1** | plan §2 (unreachable for a class of user) | `ImageUpload`'s file input sat *inside* the `role="button"` dropzone: axe `nested-interactive` on the zone and `label` on the input, 42 nodes across 18 page-visits. A keyboard user met two tab stops for one control, the second announced as an unnamed file button. This is on every screen where a HomeKrafter adds the photograph their listing lives or dies by. | One named control, one tab stop. | Sweep pass 1: `label` 22 nodes, `nested-interactive` 20. Pass 3: 0 outside the dev gallery. | — | **Fixed.** The input is a sibling of the zone now, not a child — `tabIndex={-1}` plus `aria-hidden` alone does *not* satisfy the rule (axe: "a negative tabindex inside an interactive control does not prevent assistive technologies from focusing the element"), which is worth knowing before someone reaches for the shorter fix again. |
| M26-012 | 1 | `/`, `/app-promo`, `/gallery` | anon | both | P2 | plan §7.1 (a control that does nothing) | `StoreBadges` defaulted both hrefs to `"#"` and `mealPromo` stored `"#"`, so six App Store / Google Play links scrolled the page to the top and did nothing. The apps are not published. | A claim the site can keep. | Sweep pass 1 flagged 2 dead links on `/`, 4 on `/app-promo` and `/gallery`. Pass 3: 0. | — | **Fixed.** No href renders a badge, not a link — same shape, no pointer, no tab stop, "— coming soon" in the name. `appStoreUrl`/`playStoreUrl` are optional now, so shipping day is a data change. |
| M26-013 | 1 | `/shop`, `/gifts`, `/snacks`, `/hamper`, `/search`, `/guides`, `/collections`, `/storefront` | any | 390 | P2 | plan §7.2 (390px) | `repeat(auto-fill, minmax(200px, 1fr))` cannot fit two columns in a 358px content width, so every browse grid was **one product per row**: a 501px card on an 844px screen, 1.7 products visible, 4864px of scrolling for six of them. Compounded by the announcement bar wrapping to three lines (102px) — with the header, 193px of chrome and the first product starting 588px down the opening screen. | A phone shows more than one thing you can buy. | Measured: card 501→335px, page 4864→2745px, announcement 102→38px, first card 588→523px, no horizontal scroll at 390 or 320. | — | **Fixed.** Two-up under 640px across all eight grids (plus the loading skeleton, so the skeleton still matches what it stands in for); the announcement strip is one swipeable line on a phone. |
| M26-014 | 1 | `/shop` | any | both | P2 | plan §7.1 (a control that leads nowhere) | Facets with a count of 0 — "Bakery 0", "No added sugar 0", "Karwa Chauth 0" — were fully live: a tap, a page load, an empty grid. Filter checkboxes were also the browser-default 16px, under the 24px pointer floor, in a stack of near-identical rows. | A filter that cannot return anything does not invite a tap. | Screenshots before/after in `.qa-shots/desktop/shop.png`. | — | **Fixed.** Dimmed and disabled, never hidden (a count of zero is information, and a list that reshuffles as stock changes is a list nobody can learn) — and never disabled while *checked*, or the filter that emptied the grid could not be switched off. |
| M26-015 | 1 | 6 admin detail screens | admin | both | P2 | plan §7.1 (a dead end) | Every admin "not found" state was the same one-liner in a centred card — `User not found.`, `Order not found.`, `Product not found.` — with no `h1` (so `/admin/orders/[type]/[id]` reported **zero** headings), no reason, and no way onward but the small back link above it. | What happened, why it might have, and a way out. | Sweep pass 1: `h1x0` on the admin order detail at both viewports. | — | **Fixed.** One `NotFoundCard`, used by the order, listing, collection and user screens. The order detail also gained an `h1` in its *success* state, where the reference had been a `span`. |
| M26-016 | 1 | `/storefront/[vendor]`, `/seller/reviews`, `/support`, `/admin/collections/new` | any | both | P2 | plan §2 (axe structural) | `ReviewCard` rendered `h4` under `ReviewList`'s `h2`; `SellerReviewCard` rendered `h3` directly under the page `h1`. The support chat input was placeholder-only, and the admin "add a product" select had no name. | A document outline with no holes in it. | Sweep pass 1: 2 heading jumps, 2 unnamed controls. Pass 3: 0. | — | **Fixed.** |
| M26-017 | 1 | every page (header, footer, chips) | any | both | P3 | WCAG 2.2 2.5.8 | Header icon buttons declared 38px and *rendered at 16px wide* — `width` is a suggestion inside a flex row and the search field next to them was winning, so the cart and wishlist were 16px targets on 32 routes. Nav links were 23px tall, footer legal links 18px, chips ~28px. | Targets that are the size they say they are. | Sweep pass 1: 8 undersized targets on most routes. Pass 3: a handful, all inside prose. | — | **Fixed** (`flex: none` on the icons, `min-height` on the rest). The probe also learned WCAG's inline exception, so links inside a sentence stopped being reported — a false positive per page was burying the real ones. |
| M26-018 | 1 | `/admin/orders/[type]/[id]` | admin | both | P3 | plan §7.1 (backend functionality with no UI) | "Status overrides are still M8 scope — this needs a real, audited fulfillment write." M8 shipped long ago and `PATCH /admin/orders/:type/:id/status` is implemented and audited. The admin panel was quoting an internal milestone number at an operator, about a feature the server already has. | Copy that is true, or the control. | `server/src/admin/orders.controller.ts:52` | — | **Partly fixed** in M26; **closed in M27.** The control shipped — but only after M27-003 found that the endpoint behind it would have told a buyer their order was cancelled without refunding them. The deferral turned out to be the right call for the wrong reason. |
| M27-001 | M27 | `POST /auth/social/:provider` | anon | — | **P0** | `LAUNCH-READINESS.md` §0.4 (account takeover) | The endpoint took a client-posted `email` and issued a session for whatever account matched, admin included. Confirmed exploitable against a running server on 2026-08-06 and again on 2026-08-09 before the fix. | Identity comes from a provider-signed token, or nobody gets in. | Reproduced live: `{"providerAccountId":"anything","email":"admin@homekrafted.example"}` → **200 + admin session** before; **400** after. | — | **Fixed.** `SocialTokenVerifier` verifies against Google/Apple JWKS (singleton key sets, array audience, nonce, explicit 401-vs-503 mapping). `providerAccountId`/`email` **deleted** from the DTO, so `forbidNonWhitelisted` makes the old body a structural 400 rather than a silently ignored field. Unset client id ⇒ provider off (503), so this closed with zero keys. Spec: `server/test/e2e/social-login.e2e-spec.ts` (22 cases). |
| M27-002 | M27 | `POST /auth/social/:provider` | anon | — | **P0** | discovered in review; not in the original audit | Verifying the token is **not sufficient**. `register` never sets `emailVerified`, so an attacker could register a victim's address with their own password and wait: the victim's first genuine Google sign-in was link-by-email'd straight into the attacker's account, password intact. | A provider-verified address seizes the account rather than joining it. | Spec `seizes an unverified account instead of joining it` fails on the parent. | — | **Fixed.** Auto-link only when `user.emailVerified`; otherwise revoke every `RefreshToken`, null `passwordHash`, stamp verified. Admin accounts refused outright, matching `verifyOtp`'s refusal of the OTP bypass. |
| M27-003 | M27 | `PATCH /admin/orders/:type/:id/status` | admin | — | **P0** | `CLAUDE.md` M22 (a cancellation refunds and reverses cashback) | The deferred M26-018 control could not ship as-is: `overrideStatus` writes a status and notifies, but a `cancelled` override refunds nothing, restocks nothing, reverses no cashback and leaves `cancelledAt` null — so the buyer would be told their order was cancelled and never be paid. Three further defects on the same path: re-applying `delivered` re-stamped `deliveredAt` (restarting the 7-day return window), re-selecting the current status notified the buyer again, and two admins both wrote with the last silently winning. | An override records; it never pretends to settle. | New spec fails **8 of 11** on the parent commit, all 11 pass after. | — | **Fixed.** `cancelled`/`returned` refused server-side with a message naming the refunding path; no-op guard; `deliveredAt` stamped once; `expectedStatus` ⇒ 409; audit row records `previousStatus`. Spec: `server/test/e2e/admin-status-override.e2e-spec.ts`. |
| M27-004 | M27 | error monitoring | — | — | **P1** | `PRODUCTION-AUDIT.md` §3 (largest ops gap) | Installing Sentry naively would have reported **nothing**: `AllExceptionsFilter` is a global `@Catch()` that terminates every error before Sentry's filter runs. It would have booted clean, accepted the DSN, passed a `beforeSend` unit test, and stayed silent — discovered the week it was needed. Separately, this server logs OTP codes while the SMS/email providers are stubs, and Sentry's defaults collect console output as breadcrumbs. | Errors reach Sentry; sign-in codes never do. | `test/unit/sentry-capture.spec.ts` fails if the decorator is removed. | — | **Fixed.** `@SentryExceptionCaptured()` on the existing filter; 5xx-only; no bodies, headers, cookies or breadcrumbs; email/phone/code redaction. Boot logs whether it armed. Specs: `sentry-scrub.spec.ts` (11), `sentry-capture.spec.ts` (3). |
| M27-005 | M27 | `ImageSlot` | any | both | **P1** | discovered in review | The planned cloud-storage switch would have keyed `isUpload` on a `NEXT_PUBLIC_*` build-time inline. `deploy.sh` builds on the box, so one missing line in the web `.env` would make `next/image` run its default loader against a deliberately empty `remotePatterns` and **throw** `Invalid src prop` — a render error taking out every page carrying a HomeKrafter photo, not a broken image. | Correct by construction, not by configuration. | — | — | **Fixed.** `isUpload` is structural: `/uploads/` or any absolute URL. Since `remotePatterns` is empty by design, "absolute ⇒ not optimised" is simply true and cannot be misconfigured. |
| M27-006 | M27 | `/admin/sellers` | admin | both | **P1** | plan §7.1 | Approve and Reject had no in-flight state and no disable, so a slow approval invited a second click — and approval mints an account and sends an invite. Reject sat one button-width away and fired **immediately**: a misclick sent a real applicant a permanent rejection. | A destructive action asks; a slow one says it is working. | — | — | **Fixed.** One busy flag for the queue (a refetch replaces every row, so per-row flags would be wrong), plus a confirm on Reject naming the applicant. |
| M27-007 | M27 | `/wallet` | consumer | both | P2 | M26-SWEEP §7 #1 | Top-up had no in-flight state. The `await` spans the whole Razorpay widget session — open, card details, 3-D Secure — so the button sat idle-looking for up to minutes while the amount controls stayed live. | The button says what is happening. | — | — | **Fixed.** "Waiting for payment…", amount controls disabled, reset in a `finally` so a dismissed widget re-enables it. Noted in the code that real credit is webhook-gated, so this was a double-*submit* fix, never a double-credit one. |
| M27-008 | M27 | `/admin/audit` | admin | both | P2 | `PRODUCTION-AUDIT.md` L242 claimed this existed | `GET /admin/audit` has recorded every admin mutation since M8 and had **no UI at all** — the rows were unreadable without a psql prompt, while the audit doc listed an audit log among the panel's features. | The claim made true. | — | — | **Fixed.** New route + nav entry. Filters are exactly what the endpoint supports (entity, actor, pagination) — action and date filters were cut rather than faked client-side over one page, which would have lied on the one screen whose job is completeness. Entity options are served by the API; `metadata` renders as a generic key/value grid as text. |
| M27-009 | M27 | `/admin/corporate/[id]`, `/admin/collections/[id]`, `/admin/orders/[type]/[id]` | admin | both | P2 | M26-SWEEP §7 #4/#5 | M26-015's `NotFoundCard` reached four admin screens; the corporate enquiry detail was missed and still dead-ended on `Enquiry not found.` Three screens still showed a bare `Loading…`, including the admin order detail — the most-edited screen of this milestone. The quote-line grid never stacked below `1fr 1fr`, so every field was untypeable on a phone. | No dead ends, and a skeleton that matches the page. | — | — | **Fixed.** |
| M27-010 | M27 | `/admin` | admin | both | P2 | `PRODUCTION-AUDIT.md` §5.3 (moderation SLA view) | The dashboard showed *how many* applications and listings were waiting, never *how long*. A five-day-old application is a supply problem; a count of one does not say that. | Queue age, where the operator lands. | — | — | **Fixed.** Two SLA cards above the KPI grid, each linking to the queue that clears it, terracotta past three days, and an explicit all-clear rather than "0 days". Time is captured once when the snapshot loads and passed in — never read during render (the M12 React #418 rule). |
| M28-001 | M28 | home hero, `/about` | anon | both | **P1** | plan §7.1 (no live copy states something untrue) | The hero's trust strip read "200+ home chefs · 0 preservatives · 48 hr freshly made". There are not 200 home chefs and nobody can substantiate the figure; "48 hr" also contradicted the announcement bar's "Cooked this morning" rendered two rows above it. Same family as the unconfirmed "Backed by" claims — an assertion on an indexed page that cannot be checked. | No number the site cannot stand behind. | `lib/data/site.ts:115-119`, rendered `Hero.tsx:49-56` | — | **Fixed.** Strip deleted along with `trustStats`, its `TrustStat` type and `getTrustStats()`; the hero's own sentences carry the section. Hero doc comment records why one must not come back. |
| M28-002 | M28 | every storefront + maker card | anon | both | P2 | brand review (no two maker cards share an image) | All ten vendors in `lib/data/vendors.ts` **and** in `prisma/seed.ts` pointed `avatarSrc` at `/images/vendors/avatar.jpg` and `bannerSrc` at `banner.jpg` — two files for the whole supply side, so the same stock face repeated down every grid. On a platform selling "a real person made this", one borrowed face on ten makers reads as fake faster than no face. | Distinct imagery per kitchen. | only two files exist under `public/images/vendors/` | — | **Fixed** in code as far as code can go: both fields unset, so `ImageSlot` falls back to each vendor's own labelled placeholder and no two cards match. **Real photographs are an owner asset and remain outstanding** — `CLAUDE.md` forbids generating imagery, and re-pointing several vendors at one file to fill the grid is explicitly warned against in both files. |
| M28-003 | M28 | `/about` | anon | both | P2 | M16 a11y floor (`ImageSlot` needs a real label) | The team grid rendered the literal strings "founder.jpg", "Lavya.jpg" where faces belong. Reported as broken images; nothing was broken — `ImageSlot` prints its `label` inside the placeholder and the label was a filename. | A placeholder that names the person. | `AboutClient.tsx:89,97` | — | **Fixed.** Label is the person's name, `alt=""` (the name is the next node), `sizes="64px"` (the default assumes a grid card and would pull a viewport-wide file for a 64px circle), plus an optional `photoSrc` so real photos are a data change. |
| M28-004 | M28 | `e2e/sweep.mjs` | — | both | **P1** | M26-001's rule (an instrument that passes what it cannot see) | The sweep printed `ok` beside eleven pages showing "This page didn't load". The ones that 500'd were flagged `HTTP500`, but a Next error boundary can render on a **200** — `/shop` did — so the row read clean. Found by this milestone's own sweep output. | A rendered error boundary fails the sweep whatever the status line says. | first run: 11 boundaries, 0 flagged as such | M26-001 | **Fixed.** New `ERRBOUNDARY` flag matching the "didn't load" stem shared by the four `error.tsx` titles, straight and curly apostrophes both. |
| M28-005 | M28 | `e2e/sweep.mjs` | — | both | P2 | plan §7.2 (a check that cries wolf trains people to skip it) | `unlabelledInputs` reported 12 findings across the seller editors — every one the deliberately `aria-hidden`, `tabIndex={-1}` file input that `ImageUpload`/`PhotoUpload` keep beside a named `role="button"` zone. That arrangement is the *fix* for a real nested-interactive violation, and axe reports no `label` violation on any of those routes. The field was also collected into the JSON and **never printed**, so it was invisible noise. | The probe agrees with axe, and what it measures is shown. | 12 → 0 after | — | **Fixed.** Probe skips `aria-hidden` subtrees; `input:N` added to the printed flags. |
| M28-006 | M28 | `docs/TESTS.md` browser stack | — | — | P2 | `CLAUDE.md` docs upkeep | Following the documented browser-stack setup verbatim produces an **invalid sweep**: `server/.env` carries `THROTTLE_LIMIT=20` (the code default is 120), and 174 page-visits trip it within seconds. Two full runs were thrown away before the cause was found — the second silently, because the throttled pages returned 200 with an error boundary (M28-004). | The documented procedure produces a measurable run. | 11 routes unmeasured on run 1, 15 on run 2, 0 after raising the limit | — | **Fixed.** `docs/TESTS.md` now says to raise `THROTTLE_LIMIT` for a sweep and why. |
| M28-007 | M28 | `test/unit/availability.spec.ts` | — | — | P2 | plan §11 (a test that is green for the wrong reason is worse than none) | "asks the database for today-forward blackouts only" asserted `where.date.gte <= Date.now()`. Blackouts are `@db.Date` rows at UTC midnight, so `startOfToday()` correctly builds UTC midnight from the **local** calendar date — which is *ahead of now* for the first UTC-offset hours of any day east of Greenwich. The suite was therefore green on a UTC CI runner and red on the developer's own machine overnight: ~18 hours a day. The assertion was also too weak the other way — a bound of last January satisfied it. **The code was correct; the test was not.** | An assertion about the boundary, not about the clock. | old test: green `TZ=UTC`, **red** `TZ=Asia/Kolkata` and `TZ=Pacific/Kiritimati`; new test green across UTC / Kolkata / Kiritimati / Midway / New_York | — | **Fixed.** Now asserts `gte` equals UTC midnight of the local date, and pins the boundary directly: a row stored for today is included, one for yesterday is not. |

| M28-008 | M28 | `/gallery` (component: `ProductCard`) | anon | both | P2 | M28 sweep — the one finding left after M28-004/005 | `ProductCard`'s `onCardClick` prop makes the card a `role="button"` div that still contains the wishlist and add buttons — axe's `nested-interactive`. M22 gave every real surface an `href` and the stretched-link treatment but kept the handler variant for callers with no URL, and its only caller left in the whole codebase was `/gallery`'s own demo swatch, which `notFound()`s in production. | The finding removed, not routed around. | 1 `nested-interactive` → 0 | M22's stretched-link fix | **Fixed by deletion.** The prop is gone; a card with no destination is not clickable. The gallery swatch now passes a real `href`, which is the only clickable shape the card has — and the honest demo of the stretched link. An API surface whose sole consumer is its own demo page is not an API, and leaving it kept the shape available to the next grid that wanted a card. |
| M28-009 | M28 | `e2e/sweep.mjs` | — | both | P2 | found by this sweep's own output, chasing M28-008 | Two fixtures in `F` are **cuids** (`inquiry`, `ownPlan`), regenerated by every `dropdb hk_qa && ./scripts/qa-up.sh`. A stale cuid resolves to a row that no longer exists, so `/admin/corporate/<id>` rendered the admin "No such record" card — **status 200, one `h1`, zero axe violations** — and the sweep printed `ok` for a page it had never opened. Exactly the class `ERRBOUNDARY` (M28-004) was added to close, one step further along: not an error boundary, a not-found card. | A route that resolves to nothing is never `ok`. | stale-cuid route printed `ok`; after the fix `/laundry` shows `NOTFOUND HTTP404` and the refreshed fixture resolves | M28-004 | **Fixed.** New `NOTFOUND` flag matching the three `not-found.tsx` titles — chosen over resolving the ids live because it catches *any* fixture drift (renamed slug, deleted seed row, moved route), not only the two known to rot. Both cuids refreshed, and `F` now documents that they rot, why, and the `psql` one-liners to re-read them. `/laundry` trips it correctly and already carried `HTTP404`. |
| M28-010 | M28 | `/sell` | anon | both | **P1** | reported as "seller onboarding is not working" | The fourth "How it works" step read **"Your storefront opens once HomeKrafter onboarding launches."** Live on production. Onboarding has been real since M9 (form posts to a real endpoint), M12 (an approved application becomes a full HomeKrafter with every portal module) and M17/M21 (two ways to sign in) — so the page told every applicant that the thing they were applying for did not exist yet. Step 3 also promised "our packaging + photography guide", which nobody has written, and step 2 promised "a short call" that no mechanism arranges. | The page describes what actually happens. | flow walked end to end in a browser: apply → approve → set password → sign in → own portal, **all working**; only the copy was false | — | **Fixed.** Four steps rewritten to the real mechanics. `sell.ts`'s own doc comment still described the page as future-flagged, mock and "coming soon" — rewritten, with a note not to restore the caveat: the one thing missing is a provider key, which is a server-config gap and not a reason to tell makers the product is unbuilt. Pinned by `lib/data/sell-copy.spec.ts` (red-then-green against the shipped string). |
| M28-011 | M28 | `/sell` confirmation | anon | both | P2 | found walking M28-010 | The confirmation read "We've received your application for **Test Kitchen QA**and it's now under review" — no space. JSX drops whitespace sitting between an element and a line break; the sibling waitlist branch three lines above already used the `{" "}` fix, so the two halves of the same screen disagreed. | A sentence with its spaces. | reproduced and re-verified in a browser | — | **Fixed.** Explicit `{" "}`, with a comment naming the JSX rule so it is not "tidied" back into a bare space. |
| M28-012 | M28 | `docs/TESTING.md` | — | — | P2 | `CLAUDE.md` docs upkeep | The tester walkthrough still described the M17-era flow: sign in on "the **Phone** tab of `/login?role=seller`". M21 added the set-password invite link and M25 collapsed the form to a single field, so a tester following it looked for a tab that no longer exists and never saw the invite link at all. | The documented flow is the one that runs. | — | — | **Fixed.** Rewritten to the walked flow, including the "Approved — but we could not reach them" state being *expected* on a server with no provider keys rather than a defect. |

---

## Wave 0 — proposals (max 5)

*None yet. Q1/Q3 improvements go here as proposals with a screenshot and a
one-line counter-proposal — not as work.*
